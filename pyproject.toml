[tool.ruff]
target-version = "py312"
exclude = [
    ".venv",
    "venv",
    "_aiosqlite_local_backup",
    "downloads",
]

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
